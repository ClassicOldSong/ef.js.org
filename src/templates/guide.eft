>div.{{class.body}}
	>ul.{{class.list}}
		#style = position: {{position = fixed}}; top: {{top = initial}};
		>li.{{class.expand}}
			.» Index
		+indexes
	>div.{{class.showbox}}
		>h1.{{class.title}}
			.{{title}}
		-section
